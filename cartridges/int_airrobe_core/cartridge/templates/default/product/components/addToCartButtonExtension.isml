<isset name="airrobeProps" value="${product.airrobeProps}" scope="page" />
<iscomment> TODO - priceCents is being passed in as string with decimal </iscomment>
<airrobe-opt-in
    currency="${airrobeProps.currency}"
    price-cents="${airrobeProps.priceCents}"
    rrp-cents="${airrobeProps.priceCents}"
    category="${airrobeProps.category}"
    brand="${airrobeProps.brand}"></airrobe-opt-in>
