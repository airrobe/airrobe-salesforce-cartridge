<iscomment> This is used by multi opt in widget to get the props it needs to render</iscomment>
<script>
  window.AirRobeMultiSFData = {
    appId: process.env.AIRROBE_APP_ID,
    items: "${JSON.stringify(pdict.categories.categories)}"
  };
</script>
